CONSTANTS
    Nil <- TraceNil
    (* Set constant here *)
    ResetValue <- TraceResetValue
    (* Set variables of TVOperators module *)
    Default <- DefaultImpl
    (* Set variables of TraceSpec module *)
    Vars <- vars
    BaseInit <- Init
    TraceNext <- TraceNextImpl
    MapVariables <- MapVariablesImpl

SPECIFICATION
    TraceSpec

VIEW
    TraceView

PROPERTIES
    BaseSpec

POSTCONDITION
    TraceAccepted

CHECK_DEADLOCK
    FALSE